<?php
require_once Mage::getBaseDir('lib') . '/janrain/PsrAutoloader.php';

$configModel = Mage::getModel('janrain_jump/config');

// $config = $configModel->getConfig();

// $captureConfig = new janrain\plex\CaptureConfig($config);

// $capture = new janrain\plex\Capture($captureConfig);

// echo $capture->getHtml();

echo '<hr />';

$test = $configModel->getTestConfig();

echo 'Test config value: ' . $test;
